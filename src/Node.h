#ifndef NODE_H
#define	NODE_H

#include "Node_PJS.h"
#include "PJS_Node.h"
#include "CCU_Node.h"

class Node {
public:
    Node();
    Node(const Node& orig);
    virtual ~Node();
private:

};

#endif	/* NODE_H */

